<!DOCTYPE html>
<html lang="es">
<%- include('./partials/head') %>
<body class="d-flex flex-column min-vh-100">
<%- include('./partials/header') %>

<!-- ❌ Página de error -->
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 text-center">
            <div class="mb-4">
                <i class="bi bi-exclamation-triangle text-danger" style="font-size: 5rem;"></i>
            </div>
            
            <h1 class="display-4 text-danger mb-3">
                ¡Oops! Error <%= error.status || 500 %>
            </h1>
            
            <p class="lead text-muted mb-4">
                <%= message || 'Ha ocurrido un error inesperado' %>
            </p>

            <% if (error.status === 404) { %>
                <p class="text-muted">La página que buscas no existe o ha sido movida.</p>
            <% } else if (error.status === 403) { %>
                <p class="text-muted">No tienes permisos para acceder a este recurso.</p>
            <% } else { %>
                <p class="text-muted">Nuestro equipo ha sido notificado del problema.</p>
            <% } %>

            <div class="mt-4">
                <a href="/" class="btn btn-primary me-2">
                    <i class="bi bi-house"></i> Ir al Inicio
                </a>
                <button onclick="history.back()" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Volver Atrás
                </button>
            </div>

            <!-- Mostrar stack trace solo en desarrollo -->
            <% if (error.stack && typeof error.stack === 'string') { %>
                <div class="mt-5">
                    <details class="text-start">
                        <summary class="btn btn-outline-danger btn-sm">Ver detalles técnicos</summary>
                        <pre class="bg-light p-3 rounded mt-3 text-start"><%= error.stack %></pre>
                    </details>
                </div>
            <% } %>
        </div>
    </div>
</div>
<%- include('./partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
